    .MODEL SMALL
    .STACK 32
    .DATA 
TABLE DW 12,12,12,12,12,12,12,12,12,12
    .CODE 
MAIN PROC FAR
    MOV AX, @DATA 
    MOV DS, AX

    MOV CX, 10
    MOV BX, 00H 
    MOV AX, 00H 
    LEA BX, TABLE
    MOV SI, 00H
ADDITION:  ADD AX, [BX + SI] ;TABLE[BX]
    INC SI
    INC SI
    LOOP ADDITION

    ;TO DECIMAL AND TO ASCII

    MOV CX, 10
    MOV BX, 00

DIVISION:  MOV DX,00h
    DIV CX
    ADD DX, 30H
    PUSH DX
    INC BX
    CMP AX, 0
    JNE DIVISION

    ;DISPLAY
    MOV CX, BX
PRINT: POP DX
    MOV AH, 02H
    INT 21H
    LOOP PRINT

    MOV AH, 4CH
    INT 21H

MAIN ENDP
    END MAIN
